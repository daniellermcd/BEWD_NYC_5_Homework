<div class="movie-form">
   <%= form_for @movie do |f| %>
      <% if @movie.errors.any? %>
      <div id="error-explanation">
         <h2><%= pluralize(@movie.errors.count, "error") %> prohibited the post from being saved:</h2>
         <ul>
            <% @movie.errors.full_messages.each do |msg| %>
            <li><%= msg %></li>
            <% end %>
         </ul>
      </div>
      <% end %>
      <div>
         <%= f.label :title %>
         <%= f.text_field :title %>
      </div>
      <div>
         <%= f.label :description %>
         <%= f.text_area :description %>
      </div>
      <div>
         <%= f.label :year_released %>
         <%= f.text_field :year_released %>
      </div>
      <div>
         <%= f.label :rating %>
         <%= f.text_field :rating %>
      </div>
      <div>
         <%= f.submit %>
      </div>
   <% end %>
</div>